name: Auto PR em branch nova

on:
  push:
    branches:
      - 'feature/*'
      - 'fix/*'
      - 'chore/*'
      - 'ci/*'
      - 'refactor/*'

jobs:
  auto-pr:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Cria Pull Request automaticamente
        uses: repo-sync/pull-request@v2
        with:
          source_branch: ${{ github.ref_name }}
          destination_branch: dev
          pr_title: '🚀 Auto PR: ${{ github.ref_name }} → dev'
          pr_body: |
            Esta PR foi criada automaticamente após o push da branch `${{ github.ref_name }}`.
            > Essa automação ajuda a manter o fluxo de integração contínua organizado.
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
